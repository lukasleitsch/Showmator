<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/live.css">
  <title>HTML exportieren</title>
</head>
<body<% if (items.length < 1) { %> class="on-empty"<% } %>>

  <header class="jumbo-header">
    <h1>
      <img src="/img/icon48_white.png" alt=""> <span id="title"><%= title || '' %></span> HTML-Shownotes
    </h1>
  </header>


  <div class="alert alert-info show-on-empty">
    Bitte noch etwas Geduld. Im Moment sind noch keine Shownotes eingetragen.
  </div>


  <div class="form-horizontal">

    <div class="form-group">
      <label class="control-label control-label-static">Optionen:</label>
      <div class="form-control-wrap">
        <label for="as-ul">
          <input id="as-ul" type="checkbox"> als Liste ausgeben
        </label>
        <p class="help-block">Die Shownotes werden als unsortierte Liste (<code>ul</code>) ausgegeben.</p>
      </div>
    </div>

    <div class="form-group">
      <div class="form-control-wrap form-group-no-label">
        <label for="show-times">
          <input id="show-times" type="checkbox"> Zeiten anzeigen
        </label>
        <p class="help-block">
          <label for="offset">
            Zeitpunkt des ersten Eintrags: <input type="text" id="offset" class="form-control" value="00:00:00">
          </label>
        </p>
      </div>
    </div>

    <div class="form-group form-group-no-label form-group-close-section">
      <div class="form-control-wrap">
        <label for="open-in-new-tab">
          <input id="open-in-new-tab" type="checkbox"> Links in neuen Tabs öffnen
        </label>
        <p class="help-block">Fügt jedem Link das Attribut <code>target="_blank"</code> hinzu.</p>
      </div>
    </div>

    <div class="form-group">
      <label for="sourcecode" class="control-label control-label-static">Quellcode:</label>
      <div class="form-control-wrap">
        <textarea id="sourcecode" class="form-control"></textarea>
      </div>
    </div>

  </div>


  <div id="markup-container" data-start="<%= start %>" data-offset="<%= offset || 0 %>" data-slug="<%= slug %>">
    <ul>
      <% items.forEach(function(item) { %>
        <li>
          <span class="time" data-time="<%= item.time %>"></span>
          <% if (item.isText) { %>
            <%= item.title %>
          <% } else { %>
            <a href="<%= item.url %>" target="_blank"><%= item.title %></a>
          <% } %>
        </li>
      <% }) %>
    </ul>
    <p><a href="http://showmator.com" target="_blank">Shownotes wurden mit dem Showmator erstellt</a></p>
  </div>


  <footer class="footer-author">
    <a href="http://leitsch.org">Showmator by Lukas Leitsch</a> &bull; <a href="https://leitsch.org/projects/showmator">Changelog</a> &bull;  <a href="http://flattr.com/thing/1881367/Showmator" target="_blank"><img src="/img/flattr.png" alt="Flattr this" title="Flattr this" border="0" /></a> 
  </footer>

  <script src="/js/jquery.min.js"></script>
  <script src="/socket.io/socket.io.js"></script>
  <script src="/js/live.js"></script>
</body>
</html>